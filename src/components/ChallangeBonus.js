import React from 'react';
import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles((theme) => ({

}));

function ChallangeBonus() {
  const classes = useStyles();
  return (
    <div>batata</div>
  );
}

export default ChallangeBonus;
